package HCP.Enums;

/**
 * Enum that represents calls to the call center
 * This is used to tell the call center what they should do
 * next.
 */
public enum CallCenterCall {
    EV_PATIENT_LEFT,
    WTR_PATIENT_LEFT,
    MDW_PATIENT_LEFT,
    MDR_PATIENT_LEFT,
    EXIT
}
